# frozen_string_literal: true

RSpec.describe Fastnoise2 do
  it "has a version number" do
    expect(Fastnoise2::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
